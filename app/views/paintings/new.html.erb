<% if flash[:notice]%>
  <% flash.each do |key, msg| %>
    <%= msg %>
  <% end %>
<% end %>


<%= form_for [@gallery, @painting] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :artist %>
  <%= f.text_field :artists %>

  <%= f.submit %>


 <% end %>
